<h2>All Students </h2>

<% if @students.empty? %>
  <p> There are no students in the schools. </p>
<% else %>
  <ul>
    <% @students.each do |student|%>
      <li>
        <%= link_to student.name, student_path(student) %> of house
        <%= link_to student.house.name, student.house %>.
      </li>
    <% end %>
  </ul>
<% end %>